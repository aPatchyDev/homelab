apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  prometheus.yml: |
    storage:
      tsdb:
        retention:
          size: 8GB
    scrape_configs:
      - job_name: Hypervisor
        static_configs:
          - targets: ["192.168.123.111:9100"]
